<Window x:Class="Task3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">    <DockPanel>        <!-- Add a toolbar at the top -->        <ToolBar DockPanel.Dock="Top">            <Button Content="Import" Command="{Binding ImportCommand}"/>            <Button Content="Export to Excel" Command="{Binding ExportToExcelCommand}"/>            <Button Content="Export to XML" Command="{Binding ExportToXMLCommand}"/>                    </ToolBar>        <!-- Add a filter form on the left -->        <StackPanel DockPanel.Dock="Left" Width="200" Margin="10">            <TextBlock Text="Filter by:" FontWeight="Bold"/>            <TextBlock Text="First Name"/>            <TextBox x:Name="FNameFilterTextBox" Text="{Binding FirstName,UpdateSourceTrigger=PropertyChanged}"/>            <TextBlock Text="Last Name"/>            <TextBox x:Name="LNameFilterTextBox"  Text="{Binding LastName,UpdateSourceTrigger=PropertyChanged}"/>            <TextBlock Text="Sur Name"/>            <TextBox x:Name="SNameFilterTextBox" Text="{Binding SurName,UpdateSourceTrigger=PropertyChanged}"/>            <TextBlock Text="Country"/>            <TextBox x:Name="CountryFilterTextBox" Text="{Binding Country,UpdateSourceTrigger=PropertyChanged}"/>            <TextBlock Text="City"/>            <TextBox x:Name="CityFilterTextBox"  Text="{Binding City,UpdateSourceTrigger=PropertyChanged}"/>            <TextBlock Text="Date"/>            <TextBox x:Name="DateFilterTextBox"  Text="{Binding Date,UpdateSourceTrigger=PropertyChanged}"/>            <Button Content="Apply Filter" Command="{Binding FilterCommand}" Margin="0,10,0,0"/>        </StackPanel>        <!-- Add a data grid in the remaining space -->        <DataGrid x:Name="CustomerDataGrid" ItemsSource="{Binding Records}" Margin="10" AutoGenerateColumns="True">                   </DataGrid>    </DockPanel>
</Window>
